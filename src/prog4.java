public class Prog4 {
	public static void main(String[] args) {
		// ORACLEDB LOGIN DETAILS
 		final String oracleURL = "jdbc:oracle:thin:@aloe.cs.arizona.edu:1521:oracle";
 		final String username = "user";
		final String password = "pass";

		// Create a new dbConnection object that facilitates connection to the oracle db
		// dbConnection db_conn = new dbConnection(oracleURL, username, password);
		
		// Load the Oracle JDBC Drive
		// db_conn.loadOracleJDBCDriver();

		// Establish a connection to the oracle database
		// db_conn.connect();

	}
}
